# subtest-impl

Implementation detail of the [`subtest`](https://crates.io/crates/subtest) crate - refer to it for documentation and usage.

Do not depend on this crate directly, its API is not stable.
